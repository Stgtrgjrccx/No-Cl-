fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pillow>=10.0
python-multipart>=0.0.9
python-dotenv>=1.0
# Accounts + synced history: DB (SQLite local / Postgres hosted), password
# hashing, signed session cookies. psycopg (binary) is only used when a
# Postgres DATABASE_URL is set in production.
sqlalchemy>=2.0
bcrypt>=4.0
itsdangerous>=2.0
psycopg2-binary>=2.9
# Only needed if you switch PROVIDER=anthropic (paid). Harmless to keep installed.
anthropic>=0.69
# Test-only dependencies (not needed to run the server).
pytest>=8.0
pytest-httpx>=0.30
